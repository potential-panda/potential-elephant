"""Source component v2.

This package is intentionally parallel to the existing harvester/planner code.
It defines the source catalog, ticker-source registry, availability checks,
source scheduling, and source run logging used by `cli2.py` and `api2.py`.
"""

